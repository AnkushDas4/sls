<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required: every MCP server connection and every LLM provider call is a real
         network request now (Streamable HTTP / HTTP+SSE), not a simulated round trip. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="Sednium Local Spaces"
        android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar"
        android:usesCleartextTraffic="false">

        <!-- usesCleartextTraffic is false by default/intentionally: an MCP server URL
             entered as http:// (not https://) will be blocked by Android's Network
             Security Config unless the user explicitly trusts it — see the Security
             Considerations section of the MCP implementation write-up for why this is
             kept strict rather than relaxed for convenience. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
